var readlineSync = require('readline-sync') 
let shambalam = readlineSync.question("What is your Salary :")
let otherIncomes= readlineSync.question("what are yoour other income amount :")

// salary(shambalam,otherIncomes)

// function salary(val,addUpp) {
//     var value=parseInt(val)+parseInt(addUpp)
//     console.log(`Total income will be ${value}`);
// }


salary(shambalam,otherIncomes)

var salary = function (shambalam,otherIncomes) {
    var value=parseInt(val)+parseInt(addUpp)
    console.log(`Total income will be ${value}`);
}